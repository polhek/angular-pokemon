import { RevertStringPipe } from './revert-string.pipe';

describe('RevertStringPipe', () => {
  it('create an instance', () => {
    const pipe = new RevertStringPipe();
    expect(pipe).toBeTruthy();
  });
});
